def silnia(liczba):
    if liczba == 0:
        return 1
    else:
        return liczba*silnia(liczba-1)

odp=int(input("Podaj liczbę: "))
wynik = silnia(odp)
print(wynik)
odp2=int(input("Podaj kolejną liczbę: "))
